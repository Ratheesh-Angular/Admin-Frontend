# Copy to .env.local and set values (never commit .env.local)
CBP_API_BASE_URL=https://api.flex-money.app
CBP_ADMIN_API_KEY=

# Optional: set COOKIE_SECURE=false for local HTTP dev
# COOKIE_SECURE=false
