import { AdminPlaceholder } from "@/components/pages/AdminPlaceholder";

export default function SendTransferPage() {
  return (
    <AdminPlaceholder
      title="Send Transfer"
      description="Initiate and manage outbound transfers from the admin console."
    />
  );
}
